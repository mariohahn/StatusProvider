# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'StatusProvider' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'StatusProvider', :path => './'

  # Pods for StatusProvider

  target 'StatusProviderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StatusProviderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
